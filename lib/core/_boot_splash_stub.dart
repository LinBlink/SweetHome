/// Non-web stub for [dismissBootSplash]. The real implementation lives
/// in `_boot_splash_web.dart` and is selected by the conditional import
/// in `boot_splash.dart`.
///
/// There is no HTML boot splash outside the browser — the native
/// launch screens (Android's windowBackground, iOS's LaunchScreen)
/// are torn down by the platform itself — so this is a no-op.
void dismissBootSplash() {}
